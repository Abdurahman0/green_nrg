# Telegram WebApp backend URL.
# Keep empty if frontend and backend are served from the same origin.
VITE_API_BASE_URL=

# Optional: Yandex Maps API key for the in-checkout map picker.
# If empty, the map script will still try to load (may work without a key depending on Yandex settings).
VITE_YANDEX_MAPS_API_KEY=
